# Python dependencies for PhotoWalk scripts
# Install with: pip install -r requirements.txt

opencv-python>=4.9.0
numpy>=1.26.0
Pillow>=10.3.0          # optional: for EXIF reading / HEIC conversion

# Nerfstudio (includes COLMAP wrapper and Splatfacto)
# Install separately — follow https://docs.nerf.studio/quickstart/installation.html
# Recommended: conda environment with CUDA 11.8 or 12.1
# nerfstudio>=1.1.0
